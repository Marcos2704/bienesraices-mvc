extends ../layout/index.pug
 
block content 
    div(class="py-10")
        h1(class=" my-10 text-center text-4xl font-extrabold") Your
            span(class="font-extrabold text-center text-green-700") Destiny
        h2(class="text-center text-2xl font-extrabold")= page

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= `${error ? "bg-red-500" : "bg-green-500"} py-2 px-5 rounded-lg w-full font-bold text-center text-white uppercase `)=msg
    
        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes iniciar sesion


